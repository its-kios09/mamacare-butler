name: mamacare_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '^3.8.0'

dependencies:
  http: ^1.6.0
  serverpod: 3.1.1
  serverpod_auth_idp_server: 3.1.1
  serverpod_auth_core_server: ^3.1.1
  google_generative_ai: ^0.4.6
  dotenv: ^4.2.0

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.1.1
  test: ^1.25.5


serverpod:
  scripts:
    # Starts the server and applies migrations
    start: dart bin/main.dart --apply-migrations

    # Build the Flutter web app and move it to the server's web directory
    # 
    # Unfortunately, we can't use the `-o` flag directly because of an error
    # that happens on windows. Issue is tracked in the flutter
    # repository here: https://github.com/flutter/flutter/issues/157886
    flutter_build: cd ../mamacare_flutter && flutter build web --base-href /app/ --wasm && rm -rf ../mamacare_server/web/app && mv build/web/ ../mamacare_server/web/app
